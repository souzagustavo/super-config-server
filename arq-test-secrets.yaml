my-secret-from-config-server-json: ${aws.secretsmanager:/secret/arq-config-server}
my-secret-from-config-server-url: ${aws.secretsmanager:/secret/arq-config-server:url}
my-secret-from-config-server-password: ${aws.secretsmanager:/secret/arq-config-server:password}

my-secret-from-application: ${aws.secretsmanager:/secret/application}

#my-secret1: ${aws.secretsmanager:secret/spring-boot-localstack/url}
#my-secret2: ${aws.secretsmanager:/secret/spring-boot-localstack/url}

#my-secret3: ${vault:/secret/spring-boot-localstack/url:default_value}
#my-secret4: ${vault:secret/spring-boot-localstack/url:default_value}

#y-secret5: ${aws.secretsmanager:secret/arq-config-server/url}
#my-secret6: ${aws.secretsmanager:/secret/arq-config-server/url}

#my-secret7: ${vault:/secret/arq-config-server/url:default_value}
#my-secret8: ${vault:secret/arq-config-server/url:default_value}
